import * as LuxSDK from "@lux-ai-bots/lux-sdk";

/**
 *
 * @param gameState
 * @returns {string{}} list of combinations actions possible
 *
 * @todo Compute all possible action combinations for a given Game State.
 * @body Take a game state and return all possible action combinations that the player can do. It will maybe not be used because it would be too computationally expensive and diffuicult to optimize but still usefull to have
 */
export const computeAllActionsCombination = (
  gameState: LuxSDK.GameState
): string[] => {
  return [];
};
