import * as LuxSDK from "@lux-ai-bots/lux-sdk";

/**
 * @param {LuxSDK.Player} player
 * @returns {string[]} list of actions
 *
 * @todo Compute all possible action combinations for a given player.
 * @body Take a player and return all possible action combinations that the player can do.
 */
export const computeAllActionCombinationForAPlayer = (
  player: LuxSDK.Player
): string[] => {
  return [];
};
